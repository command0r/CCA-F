# Not actually used at runtime — this demo uses httpx.MockTransport so no
# outbound calls are made. Included for consistency with the other demos
# in case you want to point retry_demo.py at the real API to compare
# against live behavior.
ANTHROPIC_API_KEY=sk-ant-your-key-here
